<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rs.elfak.mosis.nikola.myplaces">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".About"
            android:label="About"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />
        >
        <activity android:name=".MyPlacesList" android:label="My Places List" />
        <activity android:name=".EditMyPlaceActivity" android:label="Edit My Place" />
        <activity android:name=".ViewMyPlaceActivity" android:label="View My Place"></activity>
    </application>

</manifest>